export interface Baseline {
  transactionId: string;
  partnerId: string;
  salesCode:boolean;
  partnerOrderId: string;
  agentId: string;
  channel: string;
  siteId: string;
  wirecenter: string;
  intent: string;
}
